# รายงาน Assignment 1 : หาเส้นทางหยิบสินค้าที่สั้นที่สุดในโกดัง

**วิชา** 241-405 Optimization
**ไฟล์โปรแกรม** `Creat_warehouse.py`
**รันโปรแกรม** `python Creat_warehouse.py`
(ถ้าไม่อยากให้หน้าต่างกราฟเด้ง ใช้ `WH_SHOW_PLOTS=0 python Creat_warehouse.py` — รูปจะถูกเซฟเป็น
`warehouse_map.png` และ `warehouse_route.png` ให้อยู่ดี)

---

## 1. สรุปผลลัพธ์สำคัญ

| หัวข้อ | ค่า |
|---|---|
| ขนาดโกดัง | 18 × 20 (ทางเดิน 271 ช่อง, ชั้นวาง 89 ช่อง) |
| Entrance / Exit | (0, 0) / (17, 19) |
| จำนวนจุดหยิบสินค้า | n = 10 (seed = 20) |
| **จำนวนก้าวรวมที่น้อยที่สุด** | **72 ก้าว** |
| พิสูจน์ว่าเป็น optimum จริงหรือไม่ | **ใช่** — ยืนยันด้วย BFS บน state space 265,631 state |

วิธี **NN+Local Search, Genetic Algorithm, Ant Colony และ Held–Karp ได้ 72 ก้าวเท่ากันหมด**
ส่วน Nearest Neighbour เพียว ๆ ได้ 82 ก้าว (แย่กว่า 13.9%)

---

## 2. การตีความโจทย์และการสร้างแบบจำลอง (7.2.1 ส่วนแรก)

### 2.1 ประเด็นสำคัญที่ทำให้โจทย์นี้ "ไม่ใช่ TSP ธรรมดา"

โจทย์บอกว่า *"พนักงานไม่จำเป็นต้องเดินเข้าไปในช่องชั้นวาง แต่ถือว่าหยิบสินค้าได้เมื่อเดินไปยืนอยู่บน
ช่องทางเดินที่ติดกับชั้นวางนั้น"* ข้อความนี้มีผลใหญ่มาก 2 ข้อ:

**ประเด็นที่ 1 — จุดหยิบ 1 จุด มี "ที่ยืนหยิบ" ได้หลายช่อง**

เช่น จุด `(17, 6)` ยืนหยิบได้ที่ `(16,6)`, `(17,5)` หรือ `(17,7)` เลือกผิดช่องก็เสียก้าวฟรี ๆ
ปัญหานี้จึงไม่ใช่ TSP แต่เป็น **Generalized TSP-Path (GTSP)** :
แต่ละจุดหยิบ = *กลุ่ม* ของ node (เรียกว่า **access cells**) และเราต้องแตะแค่กลุ่มละ 1 node

**ประเด็นที่ 2 — เดินผ่านก็หยิบได้ฟรี**

ถ้าระหว่างเดินจาก A ไป B บังเอิญผ่านช่องที่ติดชั้นวางของจุด k อยู่แล้ว ก็ได้ของจุด k ฟรี
ไม่ต้องนับเป็นจุดแวะอีก แบบจำลอง TSP ปกติมองไม่เห็นข้อนี้ และจะรายงานระยะทาง "ยาวเกินจริง"
จึงต้องมีขั้นตอน **free-pickup reduction** มาแก้ (ดูหัวข้อ 3.6)

### 2.2 แบบจำลองกราฟ

- ทุกช่องทางเดิน (ค่า 0) = node, เชื่อมกับเพื่อนบ้าน 4 ทิศด้วย edge น้ำหนัก 1
- เนื่องจาก **น้ำหนักทุกเส้นเท่ากันหมด (= 1)** Dijkstra จะ degenerate กลายเป็น **BFS** พอดี
  จึงใช้ BFS ซึ่งเร็วกว่า (ไม่ต้องใช้ heap) และให้คำตอบเท่ากัน
  → ในโปรแกรมมีฟังก์ชัน `astar_shortest_path()` ใช้ A* + Manhattan heuristic ไว้
  **ตรวจทานว่า BFS ให้ระยะทางตรงกับ A*** (โปรแกรมพิมพ์ผลการตรวจออกมาทุกครั้งที่รัน)
- ทำ BFS จาก "node สำคัญ" ทุกตัว (entrance + exit + access cells ทั้งหมด = 24 node ตอน n=10)
  ได้ตารางระยะทางจริงในเขาวงกต ใช้เวลา 0.007 s

---

## 3. วิธีการที่เลือกใช้ และการปรับให้เข้ากับโจทย์ (7.2.1)

โปรแกรมทำไว้ **5 วิธี** (โจทย์ขออย่างน้อย 2 วิธี) + 1 ตัวตรวจคำตอบ

### 3.1 ตัวถอดรหัสร่วม : "ลำดับการหยิบ → ระยะทางจริง" ด้วย DP

ทุกวิธีข้างล่างทำงานบน **ลำดับการหยิบ (permutation)** เหมือนกันหมด แต่ลำดับอย่างเดียวยังบอก
ระยะทางไม่ได้ เพราะยังต้องเลือกว่าจุดไหนจะยืนหยิบที่ช่องไหน

แก้ด้วย **Dynamic Programming บน layered graph** (คือ shortest path บน DAG) :

```
f(t, c) = min over ช่อง c' ของขั้นที่ t-1 [ f(t-1, c') + d(c', c) ]
```

- ความซับซ้อน `O(n × A²)` โดย `A ≤ 4` จึงเร็วมาก (ใช้เป็น fitness function ได้สบาย)
- **นี่คือการปรับปรุงที่สำคัญที่สุดของงานนี้** : มันทำให้ทุก metaheuristic ได้ "ระยะทางที่ดีที่สุด
  ของลำดับนั้น ๆ" โดยอัตโนมัติ แทนที่จะต้องสุ่มเลือกช่องยืนหยิบเอง
- ถ้าไม่ทำขั้นตอนนี้ (สุ่มเลือก access cell เอง) พื้นที่ค้นหาจะใหญ่ขึ้น `4ⁿ` เท่า

### 3.2 วิธีที่ 1 : Nearest Neighbour (Greedy Constructive)

- อยู่ที่ไหนก็เดินไปหยิบจุดที่ใกล้ที่สุดที่ยังไม่ได้หยิบ
- **ปรับใช้กับโจทย์** : ระยะที่ใช้ตัดสินใจไม่ใช่ระยะเส้นตรง/Manhattan แต่เป็น **ระยะ BFS จริง
  ระหว่างกลุ่ม access cell** (`group_dist[i][j] = min` ของทุกคู่ช่อง) ถ้าใช้ระยะเส้นตรงจะโดน
  ชั้นวางหลอก เพราะจุดที่อยู่คนละฝั่งชั้นวางดูใกล้แต่ต้องเดินอ้อมยาวมาก
- ข้อดี : เร็วมาก `O(n²)` / ข้อเสีย : มองสั้น มักทิ้งจุดโดดไว้ท้ายสุดแล้วต้องวิ่งย้อนไกล

### 3.3 วิธีที่ 2 : Local Search (2-opt + Or-opt)

- **2-opt** : กลับด้านลำดับช่วง `[i..j]` → แก้เส้นทางที่ไขว้กัน
- **Or-opt** : ย้ายชิ้นส่วนยาว 1–3 จุดไปแทรกที่อื่น (กลับด้านได้ด้วย)
- **ปรับใช้กับโจทย์** : 2-opt ของ TSP มาตรฐานคำนวณ delta จากระยะแค่ 4 เส้นแล้วจบ แต่ที่นี่
  **การกลับด้านทำให้ "ช่องที่ยืนหยิบ" ของทุกจุดในช่วงนั้นเปลี่ยนไปด้วย** จะใช้สูตร delta ไม่ได้
  จึงต้องประเมินใหม่ด้วย DP ข้อ 3.1 ทุกครั้ง — ยังเร็วอยู่เพราะ DP เป็น `O(n × 16)`
- ใช้กลยุทธ์ best-improvement สลับ 2-opt ↔ Or-opt จนไม่มีอะไรดีขึ้น (ถึง local optimum)

### 3.4 วิธีที่ 3 : Genetic Algorithm (Topic 10.2)

| องค์ประกอบ | การออกแบบ |
|---|---|
| Encoding | โครโมโซม = permutation ของหมายเลขจุดหยิบ `0..n-1` (ไม่ใส่ IN/OUT เพราะตรึงเป็นหัว-ท้ายเสมอ) |
| Fitness | ระยะทางจาก DP ข้อ 3.1 (ยิ่งน้อยยิ่งดี) |
| Selection | Tournament ขนาด 3 |
| Crossover | **OX (Order Crossover)** — รักษาความเป็น permutation ไม่ให้เกิดจุดซ้ำ/หาย |
| Mutation | สุ่มระหว่าง inversion กับ swap, อัตรา 0.5 |
| Elitism | เก็บตัวดีที่สุด 2 ตัวไปรุ่นถัดไปตรง ๆ |
| ขนาด | pop = `min(200, max(60, 4n))`, generations = `min(600, max(200, 12n))` |

**สิ่งที่ปรับปรุงจาก GA มาตรฐาน (3 ข้อ)**

1. **Seeding ประชากรเริ่มต้น** — ไม่สุ่มล้วน แต่ใส่คำตอบ Nearest Neighbour 1 ตัว +
   NN ที่บังคับจุดเริ่มต่างกันอีกครึ่งประชากร ที่เหลือค่อยสุ่ม
   → ได้สมดุลระหว่าง "คุณภาพเริ่มต้น" กับ "ความหลากหลาย" ลดจำนวนรุ่นที่ต้องใช้มาก
2. **Memetic (GA + Local Search)** — ทุก 50 รุ่น เอา 3 ตัวที่ดีที่สุดไปทำ Or-opt แบบจำกัดระยะ
   (`quick_or_opt`, `O(n × window)` แทน `O(n²)`) และตอนจบรันซ้ำด้วย Local Search เต็มรูปแบบ
   → GA รับหน้าที่หา "ย่านที่ดี" ส่วน Local Search รับหน้าที่ "ขัดให้เนียน"
3. **Restart เมื่อหยุดนิ่ง (stagnation)** — ถ้า 60 รุ่นติดกันไม่มีอะไรดีขึ้น สุ่มประชากรครึ่งล่างใหม่
   → กันไม่ให้ประชากรกลายพันธุ์เหมือนกันหมดแล้วติด local optimum

> **หมายเหตุการจูน** : ตอนแรกตั้ง `elite=4, mutation=0.3, ls_every=20` ปรากฏว่าที่ n=50
> GA ลู่เข้าเร็วเกินไป (premature convergence) ได้ 122 ก้าว **แย่กว่า** NN+Local Search (104)
> หลังลด elitism เหลือ 2, เพิ่ม mutation เป็น 0.5 และทำ local search ถี่น้อยลง (ทุก 50 รุ่น)
> ผลดีขึ้นเป็น 106 ก้าว — เป็นตัวอย่างชัดเจนว่า **GA แพ้เพราะเสียความหลากหลาย ไม่ใช่เพราะ
> จำนวนรุ่นไม่พอ**

### 3.5 วิธีที่ 4 : Ant Colony Optimization (Topic 10.4)

มดแต่ละตัวสร้างลำดับการหยิบทีละจุด โดยเลือกจุดถัดไปด้วยความน่าจะเป็น

```
P(i→j) = [τ_ij ^ α] · [η_ij ^ β] / Σ(...)        α = 1, β = 3
```

จบรอบแล้วระเหยฟีโรโมน `τ *= (1−ρ)` (ρ = 0.1) แล้วโรย `Q / ระยะทาง` ลงบนเส้นที่มดใช้

**สิ่งที่ปรับปรุงจาก ACO มาตรฐาน (4 ข้อ)**

1. **η ใช้ระยะ BFS จริงระหว่างกลุ่ม access cell** ไม่ใช่ระยะยุคลิด/Manhattan ตรง ๆ
   (สำคัญมาก เพราะมีชั้นวางขวาง — จุดที่อยู่คนละฝั่งชั้นวางดูใกล้แต่จริง ๆ ไกลมาก)
2. **Elitist ant** — ทุกรอบโรยฟีโรโมนเพิ่มให้เส้นทางที่ดีที่สุดที่เคยเจอ (น้ำหนัก 2×) เร่งการลู่เข้า
3. **Max-Min Ant System** — จำกัดฟีโรโมนไว้ในช่วง `[0.05, 5.0]` กันไม่ให้เส้นใดเส้นหนึ่งครอบงำ
   จนมดทุกตัวเดินทางเดียวกันหมด (สมดุลกับข้อ 2 ที่เร่งการลู่เข้า)
4. **Hybrid กับ Local Search** — ลำดับที่ดีที่สุดตอนจบถูกส่งไปขัดด้วย 2-opt/Or-opt ต่อ

### 3.6 ขั้นตอนพิเศษ : Free-pickup reduction (ใช้กับ **ทุก** วิธี)

นี่คือการ optimize ที่มาจากการอ่านโจทย์ละเอียด ไม่ใช่อัลกอริทึมสำเร็จรูป

> ถ้าเส้นทางเดินผ่านช่องที่ติดชั้นวางของจุด k อยู่แล้ว → หยิบจุด k ได้ฟรี → ตัดจุด k ออกจาก
> "ลำดับที่ต้องแวะ" ได้เลย แล้วคำนวณเส้นทางใหม่ซึ่งจะสั้นลง

โปรแกรมจะไล่ลองลบทีละจุด ถ้าเส้นทางใหม่ **(ก) สั้นลง และ (ข) ยังหยิบครบทุกจุด** ก็ยอมรับ
แล้ววนใหม่จนลบอะไรไม่ได้อีก (ตรวจ "หยิบครบ" ด้วย bitmask `collected_mask()`)

ที่ n = 10 ชุดนี้ optimum พอดีที่ต้องแวะครบ 10 จุดอยู่แล้ว ขั้นตอนนี้จึงไม่ลดอะไร แต่ที่ n มาก ๆ
จุดหยิบเริ่มอยู่ติดกันบนชั้นวางเดียวกัน ขั้นตอนนี้จะช่วยได้เยอะ

### 3.7 ตัวเทียบ (baseline) : วิธี exact 2 แบบ

| วิธี | ใช้ทำอะไร | ความซับซ้อน | ขีดจำกัด |
|---|---|---|---|
| **Held–Karp DP** (`solve_held_karp`) | DP bitmask แบบ TSP แต่ขยาย state เป็น `f(mask, i, c)` โดย `c` = ช่องที่ยืนหยิบ → ได้คำตอบดีที่สุดของ "แบบจำลองแวะทุกจุด" | `O(2ⁿ · n² · A²)` | n ≤ 12 |
| **State-space BFS** (`solve_exact_state_bfs`) | BFS บน state `(ช่องที่ยืน, bitmask ของที่หยิบแล้ว)` → ได้ **optimum จริงของโจทย์** รวมกรณีหยิบผ่านทางฟรี | `O(ช่องทางเดิน · 2ⁿ)` | n ≤ 12 |

State-space BFS คือ "คำเฉลย" ที่ใช้ยืนยันว่า 72 ก้าวคือดีที่สุดจริง ๆ ไม่มีทางต่ำกว่านี้

---

## 4. ผลลัพธ์ที่ n = 10 (7.2.2)

### 4.1 ข้อมูลนำเข้า

```
Entrance: (0, 0)        Exit: (17, 19)
Pickup 1:(17, 6)  Pickup 2:(5, 1)   Pickup 3:(4, 17)  Pickup 4:(0, 12)  Pickup 5:(13, 17)
Pickup 6:(7, 8)   Pickup 7:(8, 17)  Pickup 8:(17, 11) Pickup 9:(13, 4)  Pickup 10:(7, 14)
```

### 4.2 ตารางเปรียบเทียบ (7.2.3)

| วิธี | ระยะก่อน reduce | **ก้าวรวม** | เวลา (s) | gap |
|---|---|---|---|---|
| 1. Nearest Neighbour (greedy) | – | 82 | 0.000 | +13.9% |
| 2. NN + Local Search (2-opt/Or-opt) | 72 | **72** | 0.009 | 0.0% |
| 3. Genetic Algorithm (memetic) | 72 | **72** | 0.146 | 0.0% |
| 4. Ant Colony Optimization | 72 | **72** | 0.039 | 0.0% |
| 5. Held–Karp DP (exact ordering) | 72 | **72** | 0.018 | 0.0% |
| **Optimum จริง (state-space BFS)** | – | **72** | 0.516 | 0.0% |

> state-space BFS ค้นหา **265,631 state** (= ช่องทางเดิน × 2¹⁰) จึงช้าที่สุดในตาราง
> ทั้งที่ n เล็กแค่ 10 — เห็นชัดว่าวิธี exact จ่ายราคาแพงแค่ไหน

### 4.3 [5.1] ลำดับการหยิบสินค้า

| ลำดับ | จุดที่ | ชั้นวาง (r, c) | ยืนหยิบที่ (r, c) |
|---|---|---|---|
| 1 | 2 | (5, 1) | (5, 2) |
| 2 | 9 | (13, 4) | (13, 3) |
| 3 | 1 | (17, 6) | (16, 6) |
| 4 | 8 | (17, 11) | (16, 11) |
| 5 | 6 | (7, 8) | (7, 9) |
| 6 | 4 | (0, 12) | (1, 12) |
| 7 | 3 | (4, 17) | (4, 16) |
| 8 | 10 | (7, 14) | (7, 15) |
| 9 | 7 | (8, 17) | (8, 16) |
| 10 | 5 | (13, 17) | (13, 16) |

สังเกตว่า DP เลือกช่องยืนหยิบได้ฉลาดมาก เช่น จุด 3 `(4,17)` เลือกยืน `(4,16)` ไม่ใช่ `(4,18)`
เพราะฝั่ง 16 เป็นทางเดินหลักที่จะเดินต่อลงไปหยิบจุด 7 และ 5 ได้พอดี

### 4.4 [5.2] เส้นทางทั้งหมด (73 ช่อง)

```
( 0, 0) -> ( 1, 0) -> ( 2, 0) -> ( 3, 0) -> ( 3, 1) -> ( 3, 2) -> ( 4, 2) -> ( 5, 2)
( 6, 2) -> ( 7, 2) -> ( 8, 2) -> ( 9, 2) -> (10, 2) -> (11, 2) -> (12, 2) -> (13, 2)
(13, 3) -> (14, 3) -> (15, 3) -> (16, 3) -> (16, 4) -> (16, 5) -> (16, 6) -> (16, 7)
(16, 8) -> (16, 9) -> (16,10) -> (16,11) -> (15,11) -> (15,10) -> (14,10) -> (13,10)
(12,10) -> (11,10) -> (10,10) -> ( 9,10) -> ( 8,10) -> ( 7,10) -> ( 7, 9) -> ( 6, 9)
( 5, 9) -> ( 4, 9) -> ( 3, 9) -> ( 2, 9) -> ( 1, 9) -> ( 1,10) -> ( 1,11) -> ( 1,12)
( 2,12) -> ( 3,12) -> ( 3,13) -> ( 3,14) -> ( 3,15) -> ( 4,15) -> ( 4,16) -> ( 5,16)
( 6,16) -> ( 7,16) -> ( 7,15) -> ( 8,15) -> ( 8,16) -> ( 9,16) -> (10,16) -> (11,16)
(12,16) -> (13,16) -> (14,16) -> (15,16) -> (16,16) -> (17,16) -> (17,17) -> (17,18)
(17,19)
```

รูปเส้นทางอยู่ในไฟล์ **`warehouse_route.png`**
(หมายเลขสีม่วงบนรูป = ลำดับการหยิบ, หมายเลขสีเหลือง = หมายเลขจุดจากโจทย์)

### 4.5 [5.3] จำนวนก้าวรวมที่น้อยที่สุด

> # **72 ก้าว**

ผ่านการตรวจสอบ 2 ชั้น
1. `collected_mask(path) == FULL_MASK` → เส้นทางหยิบครบทั้ง 10 จุดจริง
2. `cost == len(path) - 1` → จำนวนก้าวตรงกับความยาวเส้นทางที่วาดออกมาจริง

---

## 5. วิเคราะห์เมื่อเพิ่มจำนวนจุด n = 10 → 20 → 30 → 50 (7.2.4)

| n | NN | NN + Local Search | GA (memetic) | ACO | Held–Karp |
|---|---|---|---|---|---|
| 10 | 82 (0.00 s) | **72** (0.01 s) | **72** (0.15 s) | **72** (0.04 s) | **72** (0.02 s) |
| 20 | 94 (0.00 s) | 86 (0.07 s) | 86 (0.40 s) | **82** (0.13 s) | n/a |
| 30 | 116 (0.00 s) | 94 (0.24 s) | 84 (1.13 s) | **82** (0.44 s) | n/a |
| 50 | 138 (0.01 s) | 104 (1.84 s) | 106 (5.35 s) | **102** (2.43 s) | n/a |

### 5.1 ด้านคุณภาพคำตอบ

- **n = 10 ทุกวิธี (ยกเว้น NN เพียว) ได้ optimum เท่ากันหมด** — ปัญหายังเล็กเกินกว่าจะแยกฝีมือได้
- **n ≥ 20 ช่องว่างเริ่มเปิด** : ACO ชนะทุกขนาด, GA ตามมาติด ๆ, NN+LS เริ่มตามไม่ทัน
  ที่ n = 30 NN+LS ได้ 94 ส่วน ACO/GA ได้ 82–84 (**ห่างกัน ~15%**)
- เหตุผล : NN+LS เริ่มจาก **จุดตั้งต้นเดียว** พอติด local optimum ก็จบ
  ส่วน GA/ACO ค้นหาจากหลายจุดพร้อมกัน จึงหนี local optimum ได้ดีกว่าเมื่อปัญหาซับซ้อนขึ้น
- **NN เพียว ๆ แย่ลงเรื่อย ๆ ตาม n** : จาก +13.9% (n=10) เป็น +35% (n=50)
  เพราะยิ่งจุดเยอะ ยิ่งมีจุดโดดถูกทิ้งไว้ท้ายสุดมาก แล้วต้องวิ่งย้อนไกล

### 5.2 ด้านเวลาประมวลผล

| วิธี | 10 → 50 จุด | การเติบโต |
|---|---|---|
| NN | 0.00 → 0.01 s | `O(n²)` แทบไม่รู้สึก |
| NN + Local Search | 0.01 → 1.84 s | **~184×** — 2-opt เป็น `O(n²)` ต่อ 1 pass และแต่ละ pass เรียก DP `O(n)` → รวม `O(n³)` ต่อ pass |
| GA | 0.15 → 5.35 s | **~36×** — pop และ generations โตเชิงเส้นกับ n และ fitness โตเชิงเส้น → `O(n³)` |
| ACO | 0.04 → 2.43 s | **~61×** — มด n ตัว × รอบ 3n × สร้างลำดับ `O(n²)` → `O(n⁴)` แต่ค่าคงที่เล็ก |
| Held–Karp | 0.02 s → n/a | `O(2ⁿ · n² · A²)` — **ตายที่ n ≈ 13** |
| State-space BFS | 0.52 s → n/a | `O(cells · 2ⁿ)` — n=10 ใช้ 265k state, n=50 ต้องใช้ ~3×10¹⁷ state |

### 5.3 สรุปเชิงปฏิบัติ

1. **วิธี exact ใช้ได้เฉพาะ n เล็ก (≤ ~12)** เพราะ state space โตแบบ `2ⁿ`
   n = 50 ต้องใช้หน่วยความจำระดับ 10¹⁷ state — เป็นไปไม่ได้
   แต่มัน **มีค่ามากในฐานะตัวเทียบ** : ทำให้เรารู้ว่าที่ n=10 heuristic ได้ optimum จริง ไม่ใช่เดา
2. **ACO คุ้มที่สุดโดยรวม** — ได้คำตอบดีที่สุดทุกขนาด และเร็วกว่า GA ราว 2 เท่า
   เหตุผลที่ ACO เหมาะกับโจทย์นี้เป็นพิเศษ : ฟีโรโมนสะสมบน "คู่จุดที่ควรเดินติดกัน" ซึ่งตรงกับ
   โครงสร้างโกดังแบบช่องทางเดินยาว (จุดในอาเซิลเดียวกันควรหยิบติดกัน) มดจึงเรียนรู้ได้เร็ว
3. **GA ต้องจูนเยอะกว่า** — ไวต่อ elitism/mutation rate มาก (ดูหมายเหตุการจูนหัวข้อ 3.4)
   และช้ากว่าเพราะต้องประเมิน fitness ทั้งประชากรทุกรุ่น
4. **NN + Local Search คือตัวเลือกที่คุ้มที่สุดถ้าต้องการคำตอบเร็วทันที** ที่ n=10 ได้ optimum
   ภายใน 0.009 s (เร็วกว่า exact 57 เท่า) เหมาะกับระบบจริงที่ต้องตอบทันทีที่พนักงานสแกนออเดอร์
5. **ถ้า n ใหญ่กว่านี้มาก (100+)** ควรใช้ ACO/GA แบบ hybrid ต่อไป และเพิ่ม
   - ตัด candidate list ให้มดเลือกเฉพาะ k เพื่อนบ้านที่ใกล้ที่สุด (ลด `O(n²)` เป็น `O(nk)`)
   - แทน 2-opt เต็มรูปแบบด้วย Or-opt แบบจำกัดระยะ (`quick_or_opt` ในโค้ด)

---

## 6. โครงสร้างโค้ด

| ส่วน | บรรทัดในไฟล์ | หน้าที่ |
|---|---|---|
| 1–6 | แผนที่, movements, validate, สุ่มจุดหยิบ, วาดแผนที่ | **ของเดิมจากโจทย์** (คงไว้ทั้งหมด เพิ่มแค่ฟังก์ชันวาดเส้นทาง) |
| 7 | `get_access_cells`, `bfs_shortest_paths`, `astar_shortest_path` | แบบจำลองกราฟ + BFS/A* (Topic 08) |
| 8 | `WarehouseProblem` | ตารางระยะทางระหว่าง node สำคัญ + bitmask ว่าช่องไหนหยิบของชิ้นไหนได้ |
| 9 | `evaluate_order`, `build_route`, `collected_mask` | ตัวถอดรหัส ลำดับ → เส้นทางจริง (DP) |
| 10 | `solve_nearest_neighbour` | วิธีที่ 1 |
| 11 | `two_opt`, `or_opt`, `local_search`, `quick_or_opt` | วิธีที่ 2 |
| 12 | `solve_genetic` | วิธีที่ 3 (Topic 10.2) |
| 13 | `solve_ant_colony` | วิธีที่ 4 (Topic 10.4) |
| 14 | `solve_held_karp` | วิธีที่ 5 — exact |
| 15 | `free_pickup_reduction` | ขั้นตอน optimize เพิ่มที่มาจากการอ่านโจทย์ |
| 16 | `solve_exact_state_bfs` | ตัวพิสูจน์ optimum จริง |
| 17–19 | `run_method`, `print_result_detail`, `scaling_experiment`, main | รัน/เปรียบเทียบ/แสดงผล |

### ไฟล์ผลลัพธ์
- `warehouse_map.png` — แผนที่โกดังพร้อมจุดต่าง ๆ (รูปเดียวกับในโจทย์)
- `warehouse_route.png` — แผนที่ + เส้นทางคำตอบ 72 ก้าว + ลำดับการหยิบ
